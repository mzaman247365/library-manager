# Database Configuration
# Valid values for DB_TYPE: 'neon' or 'postgres'
DB_TYPE=neon

# Note for Neon PostgreSQL connections:
# WebSockets are automatically used for all Neon connections in serverless environments.
# No additional configuration is required.

# Neon Database credentials
DATABASE_URL=postgres://user:password@host/database
# Or use this alternative format with the same credentials
NEON_DATABASE_URL=postgres://user:password@host/database

# Standard PostgreSQL Database credentials (if using DB_TYPE=postgres)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres_password
PGDATABASE=library

# Session configuration
SESSION_SECRET=your-session-secret-key

# Server configuration
PORT=5000
NODE_ENV=development